require "spec_helper"

describe Belugas::Python do
  it "has a version number" do
    expect(Belugas::Python::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
